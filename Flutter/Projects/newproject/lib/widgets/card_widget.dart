import 'package:flutter/material.dart';

cardWidget(context) {
  return Container(
    margin: const EdgeInsets.all(9),
    width: MediaQuery.of(context).size.width * 0.45,
    height: MediaQuery.of(context).size.height * 0.25,
    color: Color(0xffFD297A),
  );
}
