import 'dart:io';

void main() {
  for (var i = 1; i <= 129; i++) {
    //print(4 * i);
  }
  // 1. Initialize
  // 2. Check Condition
  // 3. if condition True, execute statements
  // 4. Increment / Decrement

  var names = ["Huzaifa", "Osama", "Uzair"];
  for (var a in names) {
    print(a);
  }
}
